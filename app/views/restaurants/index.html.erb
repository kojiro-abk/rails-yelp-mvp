<h2>List of restaurants</h2>

<% @restaurants.each do |restaurant| %>
  <li><%= link_to "#{restaurant.name} - #{restaurant.category}", restaurant_path(restaurant) %></li>
<% end %>
<%= link_to "Add new restaurant", new_restaurant_path %>